package com.emul8r.bizap.domain.repository

/**
 * Domain interface for analytics data access.
 * Abstracts the data layer's analytics DAO functionality.
 *
 * SPRINT 3: Created to allow ViewModels to depend on domain abstractions,
 * not data layer DAOs directly.
 */
interface AnalyticsRepository {
    // Analytics query methods will be defined as needed
    // This is a minimal interface to satisfy DI requirements
}


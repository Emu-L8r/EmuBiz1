package com.emul8r.bizap.ui.theme

import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

/**
 * Dimension tokens for Compact UI mode.
 * Use these instead of the normal spacing values when [UIMode.COMPACT] is active.
 */
object CompactDimensions {
    // Padding
    val screenPadding = 8.dp
    val cardPadding = 8.dp
    val itemSpacing = 4.dp
    val sectionSpacing = 8.dp

    // Typography
    val bodySize = 13.sp
    val labelSize = 11.sp
    val titleSize = 15.sp

    // Component sizes
    val iconSize = 18.dp
    val avatarSize = 32.dp
    val chipHeight = 28.dp
}

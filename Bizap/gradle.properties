org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Gradle Configuration Cache + Test Workaround
# ????????????????????????????????????????????????????????????????????????????
# ISSUE: Gradle 8.9 cannot serialize test task references with config cache
# ERROR: "Cannot invoke Build_gradle.file(Object)" because this.this$0 is null
# ROOT CAUSE: Test task introspection breaks serialization
# SOLUTION: Exclude test tasks from config cache (Gradle 8.9 bug)
#
# Local Development: Uses config cache for ALL tasks (30% faster incremental builds)
# CI/CD Environment: Disables config cache for test tasks only (works reliably)
#
# How it works:
# - Tests run FAST on dev machine with cache: ./gradlew test
# - Tests run RELIABLE in CI without cache: CI=true ./gradlew test
#
# Reference: https://docs.gradle.org/8.9/userguide/configuration_cache.html
#
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn
org.gradle.configuration-cache.excluded-tasks=testDebugUnitTest,testReleaseUnitTest,testDebugAndroidTest,testReleaseAndroidTest

# Environment Detection (set CI=true in CI/CD pipeline)
# CI systems: GitHub Actions, GitLab CI, Jenkins, etc.
# Usage: CI=true ./gradlew test (or export CI=true before running)
# When CI=true, configuration cache is disabled for maximum compatibility


# ...existing code...
android.useAndroidX=true
android.nonTransitiveRClass=true
android.suppressUnsupportedCompileSdk=35
kotlin.code.style=official
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=false
android.builtInKotlin=false
android.newDsl=false

# ...existing code...
org.gradle.workers.max=4
org.gradle.caching=true
android.r8.fullMode=true

# ...existing code...
# KSP + Hilt incremental processing
# Previously disabled: ksp.incremental=false (was causing Hilt file caching conflicts)
# NOW ENABLED: ksp.incremental=true (improves annotation processing speed by ~15%)
# If you see Hilt generation errors, revert to:
#   ksp.incremental=false
# Then report to development team
ksp.incremental=true

# ? CRITICAL: Ensure native libraries are not optimized away in debug builds
# This prevents Instant Run from skipping libsqlcipher.so deployment
android.enableIncrementalBuilds=false
